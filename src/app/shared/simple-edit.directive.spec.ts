import { SimpleEditDirective } from './simple-edit.directive';

describe('SimpleEditDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleEditDirective();
    expect(directive).toBeTruthy();
  });
});
